(ns clojure-kata.core
  (:gen-class))

(defn cell-should-survive [num-neighbours]
  (throw (Exception. "Unsupported operation")))

;; Later...
;; (defn evolve-grid [live-cell-set]
;;  #{})

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
